#1 git rebase -i <Commit_ID>
#2 .gitignore













git rebase - <Commit_ID>

            --reword        Rename a commit message
            --drop          Delete a commit
            --squash        